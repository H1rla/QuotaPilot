"""OpenAI Codex provider: transport, parsing, and the `UsageProvider` adapter.

Verified against the locally installed `codex` CLI (codex-cli 0.154.0); see
`docs/DECISIONS.md` for what was verified versus assumed, and
`tests/fixtures/openai_codex/PROVENANCE.md` for fixture sources.
"""
